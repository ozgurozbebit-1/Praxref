# Mimari
`src/app` App Router sayfaları, `src/components` ortak UI, `src/lib` demo veri; `prisma` PostgreSQL modeli içerir. MVP ekranları demo veri kullanır. Gerçek akışta GameTrial verileri Session/GameRun ile ilişkilendirilir, CognitiveScore 0–100 normalize hesaplanır.
