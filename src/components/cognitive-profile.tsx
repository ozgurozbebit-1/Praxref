"use client";
import { useEffect, useMemo, useState } from "react"; import { Card, TrendIndicator } from "@/components/ui"; import { createRuleBasedSummary } from "@/lib/clinical-summary/rule-based-summary"; import { getLatestDomainScores, getRecentUnifiedSessions } from "@/lib/session-store/unified-session-store"; import type { CognitiveDomainScore } from "@/lib/protocol/cognitive-domain";
const labels = { sustainedAttention: "Odak Sürekliliği", selectiveAttention: "Seçici Dikkat", inhibition: "İnhibisyon Kontrolü", workingMemory: "Çalışma Belleği" }; const demo: CognitiveDomainScore[] = [{ domain: "sustainedAttention", score: 78, confidence: "moderate", trialCount: 26, validTrialCount: 24, lastUpdated: null, trend: "improving", dataQuality: [], audience: "kids" }, { domain: "selectiveAttention", score: 73, confidence: "moderate", trialCount: 22, validTrialCount: 20, lastUpdated: null, trend: "stable", dataQuality: [], audience: "kids" }, { domain: "inhibition", score: 81, confidence: "moderate", trialCount: 25, validTrialCount: 22, lastUpdated: null, trend: "stable", dataQuality: [], audience: "kids" }, { domain: "workingMemory", score: 76, confidence: "moderate", trialCount: 21, validTrialCount: 20, lastUpdated: null, trend: "improving", dataQuality: [], audience: "kids" }];
export function CognitiveProfile() { const [audience, setAudience] = useState<"kids" | "teen">("kids"), [scores, setScores] = useState<CognitiveDomainScore[]>(demo), [sessions, setSessions] = useState<ReturnType<typeof getRecentUnifiedSessions>>([]); useEffect(() => { const next = getLatestDomainScores(audience), hasData = next.some(score => score.score !== null); setScores(hasData ? next : demo); setSessions(getRecentUnifiedSessions().filter(session => session.audience === audience).slice(0, 4)); }, [audience]); const summary = useMemo(() => createRuleBasedSummary(scores), [scores]); return <section className="cognitive-profile"><div className="profile-heading"><div><p className="eyebrow">PRAXREF Bilişsel Profil</p><h2>Demo Çocuk</h2><p className="muted">Yerel demo verisi · segment içi görünüm</p></div><div className="profile-filter" aria-label="Yaş segmenti filtresi"><button className={audience === "kids" ? "is-active" : ""} onClick={() => setAudience("kids")}>Kids</button><button className={audience === "teen" ? "is-active" : ""} onClick={() => setAudience("teen")}>Teen</button></div></div><div className="domain-grid">{scores.map(score => <Card key={score.domain} className="domain-card"><p>{labels[score.domain]}</p><strong>{score.score ?? "—"}</strong><span>{score.confidence === "insufficient" ? "Yetersiz veri" : `${score.confidence} güven`}</span><TrendIndicator label={score.trend === "insufficient-data" ? "Veri yok" : score.trend === "improving" ? "Yükseliyor" : score.trend === "declining" ? "Değişken" : "Stabil"} direction={score.trend === "improving" ? "up" : score.trend === "declining" ? "down" : "flat"} />{score.dataQuality.length > 0 && <small>Veri kalitesi uyarısı</small>}</Card>)}</div><Card className="profile-summary"><h3>Ölçülen oyun performansı özeti</h3>{summary.map(line => <p key={line}>{line}</p>)}</Card><Card className="recent-unified"><h3>Son 4 oyun oturumu</h3>{sessions.length ? sessions.map(session => <div key={session.sessionId}><span>{labels[session.domain]}</span><b>{session.score ?? "—"}</b><small>{session.audience === "kids" ? "Kids" : "Teen"} · yerel veri</small></div>) : <p>Demo veri gösteriliyor; tamamlanan yerel oturumlar burada görünür.</p>}</Card></section>; }
