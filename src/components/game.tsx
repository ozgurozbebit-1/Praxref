"use client"; import { useState } from "react"; import { GameArtwork } from "@/components/artwork"; import { Button, ProgressBar } from "@/components/ui";
type GameKey = "focus-hunt" | "selective-attention" | "inhibition" | "working-memory";
export function Game({ title, game, audience = "kids", instruction }: { title: string; game: GameKey; audience?: "kids" | "teen"; instruction: string }) { const [started, setStarted] = useState(false); return <section className="game"><GameArtwork game={game} audience={audience} aspect="hero" priority alt={`${title} oyun dünyası için hero görseli`} className="game__artwork" /><p className="eyebrow">Bugünkü görev</p><h1>{title}</h1><p>{started ? "Harika gidiyorsun. Oyun mekanikleri sonraki sürümde burada olacak." : instruction}</p><ProgressBar value={started ? 100 : 35} label="Bugünkü görev ilerlemesi" /><Button variant="secondary" onClick={() => setStarted(!started)}>{started ? "Bir tur daha" : "Hazırım"}</Button><small>Bu ekranda henüz performans verisi kaydedilmez.</small></section>; }
