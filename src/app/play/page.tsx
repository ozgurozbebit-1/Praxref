import Link from "next/link"; import { CharacterArtwork } from "@/components/artwork"; import { Shell } from "@/components/shell"; import { audienceConfig } from "@/lib/audience"; import { Badge } from "@/components/ui";
export default function Play() { return <Shell play><section className="play-hero"><div><p className="eyebrow">Oyun merkezi</p><h1>Hangi PRAXREF dünyası senin için?</h1><p>Yaşına ve deneyim tarzına uygun alanı seç.</p></div><CharacterArtwork audience="kids" character="duo" state="welcome" alt="Praxref Kids karakter alanı" className="play-hero__character" /></section><section className="segment-grid segment-grid--play">{(Object.entries(audienceConfig) as [keyof typeof audienceConfig, typeof audienceConfig[keyof typeof audienceConfig]][]).map(([audience, config]) => <Link href={`/play/${audience}`} key={audience} className={`segment-card segment-card--${audience}`}><Badge tone={audience === "kids" ? "coral" : "lilac"}>{config.ages}</Badge><h2>{config.label}</h2><p>{config.description}</p><strong>{config.characters.join(" & ")}</strong><span>{audience === "kids" ? "Keşfe başla →" : "Challenge'a geç →"}</span></Link>)}</section></Shell>; }
