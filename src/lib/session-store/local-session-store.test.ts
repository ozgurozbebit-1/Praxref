import { describe, expect, it } from "vitest"; import { trial } from "@/features/focus-hunt/fixtures/trials"; import { buildFocusHuntSummary } from "@/features/focus-hunt/scoring/focus-hunt"; import { localSessionStore } from "./local-session-store";
describe("local session store", () => { it("keeps trials and summaries in memory when storage is unavailable", () => { localSessionStore.saveTrial(trial()); const summary = buildFocusHuntSummary("local", "kids", [trial()], 1, 1); localSessionStore.saveSessionSummary(summary); expect(localSessionStore.getRecentSessions()[0]?.sessionId).toBe("local"); }); });
