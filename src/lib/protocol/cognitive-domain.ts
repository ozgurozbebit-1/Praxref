export type CognitiveDomain = "sustainedAttention" | "selectiveAttention" | "inhibition" | "workingMemory"; export type Confidence = "insufficient" | "low" | "moderate" | "good"; export type DomainTrend = "improving" | "stable" | "declining" | "insufficient-data"; export type DataQualityFlag = "too-fast-responses" | "invalid-reaction-times" | "incomplete-session" | "too-few-trials" | "storage-fallback" | "excessive-omissions" | "excessive-commissions" | "unstable-response-time";
export type CognitiveDomainScore = { domain: CognitiveDomain; score: number | null; confidence: Confidence; trialCount: number; validTrialCount: number; lastUpdated: string | null; trend: DomainTrend; dataQuality: DataQualityFlag[]; audience: "kids" | "teen" };
export const domainForGame: Record<"focus-hunt" | "selective-attention" | "inhibition" | "working-memory", CognitiveDomain> = { "focus-hunt": "sustainedAttention", "selective-attention": "selectiveAttention", inhibition: "inhibition", "working-memory": "workingMemory" };
export const confidenceThresholds = { low: 10, moderate: 20, good: 40 } as const;
export const confidenceFor = (validTrialCount: number): Confidence => validTrialCount < confidenceThresholds.low ? "insufficient" : validTrialCount < confidenceThresholds.moderate ? "low" : validTrialCount < confidenceThresholds.good ? "moderate" : "good";
