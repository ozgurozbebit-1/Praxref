import type { CognitiveDomain, DataQualityFlag } from "./cognitive-domain";
export type GameType = "focus-hunt" | "selective-attention" | "inhibition" | "working-memory"; export type ProtocolAudience = "kids" | "teen";
export type UnifiedSession = { sessionId: string; childId?: string; audience: ProtocolAudience; gameType: GameType; startedAt: string; completedAt: string; durationSec: number; difficultyStart: number; difficultyEnd: number; trialCount: number; validTrialCount: number; score: number | null; dataQualityFlags: DataQualityFlag[]; completed: boolean; domain: CognitiveDomain; metrics: Partial<{ accuracyPercent: number; omissionRate: number; commissionRate: number; meanReactionTimeMs: number; medianReactionTimeMs: number; reactionTimeSD: number; reactionTimeCV: number }> };
