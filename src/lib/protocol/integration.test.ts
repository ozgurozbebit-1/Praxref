import { describe, expect, it } from "vitest"; import { confidenceFor, createRuleBasedSummary, normalizeFocusHunt, validateUnifiedSession } from "@/lib/protocol";
const session = normalizeFocusHunt({ sessionId: "one", audience: "kids", totalTrials: 12, lateResponses: 0, sustainedAttentionScore: 75, accuracyPercent: 80, omissionErrors: 1, commissionErrors: 1, currentDifficulty: 2, completedDurationSec: 180, insufficientData: false });
describe("unified protocol", () => { it("maps a game summary", () => expect(session.gameType).toBe("focus-hunt")); it("maps its cognitive domain", () => expect(session.domain).toBe("sustainedAttention")); it("accepts a valid normalized session", () => expect(validateUnifiedSession(session)).toBe(true)); it("rejects scores above 100", () => expect(validateUnifiedSession({ ...session, score: 101 })).toBe(false)); it("rejects negative trial counts", () => expect(validateUnifiedSession({ ...session, trialCount: -1 })).toBe(false)); it("rejects invalid audiences", () => expect(validateUnifiedSession({ ...session, audience: "other" as "kids" })).toBe(false)); it("derives insufficient confidence", () => expect(confidenceFor(9)).toBe("insufficient")); it("derives low confidence", () => expect(confidenceFor(10)).toBe("low")); it("derives moderate confidence", () => expect(confidenceFor(20)).toBe("moderate")); it("derives good confidence", () => expect(confidenceFor(40)).toBe("good")); it("creates a non-medical insufficient-data summary", () => expect(createRuleBasedSummary([{ domain: "workingMemory", score: null, confidence: "insufficient", trialCount: 0, validTrialCount: 0, lastUpdated: null, trend: "insufficient-data", dataQuality: [], audience: "kids" }])[0]).toContain("yeterli")); });
