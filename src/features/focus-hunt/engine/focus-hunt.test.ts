import { describe, expect, it } from "vitest"; import { classifyTrial, shouldAcceptResponse } from "./focus-hunt"; import { getReactionTimeQualityFlag, getReactionTimeStats } from "./reaction-time"; import { trial } from "../fixtures/trials";
const base = () => { const source = trial(); const { respondedAt, reactionTimeMs, reactionTimeQualityFlag, responded, correct, errorType, ...rest } = source; return rest; };
describe("trial classification", () => { it("classifies target responses, omissions, commissions and late responses", () => { expect(classifyTrial(base(), 1400, 600).correct).toBe(true); expect(classifyTrial(base(), null, 600).errorType).toBe("omission"); expect(classifyTrial({ ...base(), isTarget: false }, 1400, 600).errorType).toBe("commission"); expect(classifyTrial(base(), 1800, 600).errorType).toBe("late-response"); }); it("accepts only the first double response", () => { expect(shouldAcceptResponse(false)).toBe(true); expect(shouldAcceptResponse(true)).toBe(false); }); it("treats no-response distractors as correct inhibition", () => expect(classifyTrial({ ...base(), isTarget: false }, null, 600).correct).toBe(true)); it("flags invalid and too-fast RTs", () => { expect(getReactionTimeQualityFlag(-1)).toBe("invalid"); expect(getReactionTimeQualityFlag(90)).toBe("too-fast"); expect(getReactionTimeStats([100, 200, null]).median).toBe(150); }); });
