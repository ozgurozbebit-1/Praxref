import { getReactionTimeQualityFlag } from "./reaction-time"; import type { FocusHuntStimulus, FocusHuntTrial, StimulusType } from "../types";
export function createStimulus(targetRate: number): FocusHuntStimulus { const isTarget = Math.random() < targetRate; const distractors: StimulusType[] = ["circle", "diamond", "spark"]; return { isTarget, type: isTarget ? "star" : distractors[Math.floor(Math.random() * distractors.length)] }; }
export const shouldAcceptResponse = (alreadyResponded: boolean) => !alreadyResponded;
export function classifyTrial(base: Omit<FocusHuntTrial, "respondedAt" | "reactionTimeMs" | "reactionTimeQualityFlag" | "responded" | "correct" | "errorType">, respondedAt: number | null, responseWindowMs: number): FocusHuntTrial { if (respondedAt === null) return { ...base, respondedAt: null, reactionTimeMs: null, reactionTimeQualityFlag: null, responded: false, correct: !base.isTarget, errorType: base.isTarget ? "omission" : null }; const rawReactionTimeMs = Math.round(respondedAt - base.appearedAt); const reactionTimeQualityFlag = getReactionTimeQualityFlag(rawReactionTimeMs); const reactionTimeMs = Math.max(0, rawReactionTimeMs); if (!base.isTarget) return { ...base, respondedAt, reactionTimeMs, reactionTimeQualityFlag, responded: true, correct: false, errorType: "commission" }; if (reactionTimeMs > responseWindowMs) return { ...base, respondedAt, reactionTimeMs, reactionTimeQualityFlag, responded: true, correct: false, errorType: "late-response" }; return { ...base, respondedAt, reactionTimeMs, reactionTimeQualityFlag, responded: true, correct: true, errorType: null }; }
