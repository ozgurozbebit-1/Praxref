import{getReactionTimeQualityFlag}from"@/features/focus-hunt";import type{InhibitionTrial,TrialType}from"../types";export const acceptResponse=(already:boolean)=>!already;export function createTrial(goRate:number):{type:TrialType;label:string;icon:string}{const type=Math.random()<goRate?"go":"no-go";return{type,label:type==="go"?"GİT · Rokete dokun":"DUR · Bekle",icon:type==="go"?"🚀":"✋"}}export function classify(base:Omit<InhibitionTrial,"respondedAt"|"reactionTimeMs"|"responded"|"correct"|"errorType"|"responseQualityFlag">,respondedAt:number|null,windowMs:number):InhibitionTrial{if(respondedAt===null)return{...base,respondedAt:null,reactionTimeMs:null,responseQualityFlag:null,responded:false,correct:base.trialType==="no-go",errorType:base.trialType==="go"?"omission":null};const raw=Math.round(respondedAt-base.appearedAt),rt=Math.max(0,raw),flag=getReactionTimeQualityFlag(raw);if(raw<0||flag==="too-fast")return{...base,respondedAt,reactionTimeMs:rt,responseQualityFlag:flag,responded:true,correct:false,errorType:"premature-response"};if(rt>windowMs)return{...base,respondedAt,reactionTimeMs:rt,responseQualityFlag:flag,responded:true,correct:false,errorType:"late-response"};if(base.trialType==="no-go")return{...base,respondedAt,reactionTimeMs:rt,responseQualityFlag:flag,responded:true,correct:false,errorType:"commission"};return{...base,respondedAt,reactionTimeMs:rt,responseQualityFlag:flag,responded:true,correct:true,errorType:null}}
