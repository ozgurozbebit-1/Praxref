import type{InhibitionAudience}from"../types";export const INHIBITION_SESSION_SECONDS=180,INHIBITION_BLOCK_SIZE=10;const c={kids:{initial:2,window:1650,min:900,size:120,go:.78},teen:{initial:4,window:1300,min:750,size:92,go:.7}}as const;export function params(a:InhibitionAudience,l:number){const b=c[a],n=Math.max(1,Math.min(10,l));return{windowMs:Math.max(b.min,b.window-(n-1)*90),size:Math.max(58,b.size-(n-1)*3),goRate:Math.max(.6,b.go-(n-1)*.02),interval:Math.max(280,580-(n-1)*25)}}
