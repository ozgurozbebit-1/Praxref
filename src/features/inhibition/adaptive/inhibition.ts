import{calculateCommissionScore,calculateGoAccuracyScore,calculateInhibitionAccuracyScore,calculateReactionStabilityScore}from"../scoring/inhibition";import type{InhibitionTrial}from"../types";export const clampDifficulty=(n:number)=>Math.max(1,Math.min(10,n));export function nextDifficulty(l:number,t:InhibitionTrial[]){if(t.length<10)return l;const i=calculateInhibitionAccuracyScore(t),c=100-calculateCommissionScore(t),g=calculateGoAccuracyScore(t),s=calculateReactionStabilityScore(t);if(i>=90&&c<=10&&g>=85&&s>=55)return clampDifficulty(l+1);if(i<75||c>20||g<75)return clampDifficulty(l-1);return l}
