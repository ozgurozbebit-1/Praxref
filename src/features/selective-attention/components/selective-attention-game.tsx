"use client"; import { useEffect, useRef, useState } from "react"; import { GameArtwork } from "@/components/artwork"; import { Button, ProgressBar } from "@/components/ui"; import { SELECTIVE_ATTENTION_BLOCK_SIZE, SELECTIVE_ATTENTION_SESSION_SECONDS, buildSelectiveSummary, classifySelectiveTrial, createTrialSet, getNextDifficulty, getSelectiveParams, type SelectiveAttentionAudience, type SelectiveAttentionTrial, type Stimulus, type TrialType } from "@/features/selective-attention"; import { selectiveAttentionStore } from "../local-session-store";
type Phase = "intro" | "countdown" | "playing" | "complete"; const makeId = () => `selective-${Math.random().toString(36).slice(2)}`;
export function SelectiveAttentionGame({ audience }: { audience: SelectiveAttentionAudience }) { const [phase, setPhase] = useState<Phase>("intro"), [count, setCount] = useState(3), [rule, setRule] = useState(""), [stimuli, setStimuli] = useState<Stimulus[]>([]), [difficulty, setDifficulty] = useState(audience === "kids" ? 2 : 4), [trials, setTrials] = useState<SelectiveAttentionTrial[]>([]), [remaining, setRemaining] = useState(SELECTIVE_ATTENTION_SESSION_SECONDS), [feedback, setFeedback] = useState(""); const sessionId = useRef(makeId()), base = useRef<Omit<SelectiveAttentionTrial, "respondedAt" | "reactionTimeMs" | "selectedStimulus" | "responded" | "correct" | "errorType" | "reactionTimeQualityFlag"> | null>(null), seen = useRef(false), trialsRef = useRef<SelectiveAttentionTrial[]>([]), levelRef = useRef(difficulty), endAt = useRef(0), timer = useRef<ReturnType<typeof setTimeout> | null>(null);
 const clear = () => { if (timer.current) clearTimeout(timer.current); }; const finish = () => { clear(); selectiveAttentionStore.saveSessionSummary(buildSelectiveSummary(sessionId.current, audience, trialsRef.current, levelRef.current)); setStimuli([]); setPhase("complete"); }; const show = (level = levelRef.current) => { if (performance.now() >= endAt.current) return finish(); seen.current = false; const params = getSelectiveParams(audience, level), type: TrialType = Math.random() < params.conjunctionRate ? "conjunction" : params.similarity > .55 && Math.random() < .45 ? "similarity" : "feature", set = createTrialSet(type, params.distractorCount, params.similarity), appearedAt = performance.now(); base.current = { trialId: makeId(), sessionId: sessionId.current, audience, difficultyLevel: level, trialType: type, targetRule: set.rule, targetStimulus: set.target, distractorCount: params.distractorCount, distractorSimilarity: params.similarity, appearedAt }; setRule(set.rule); setStimuli(set.stimuli); timer.current = setTimeout(() => record(null), params.responseWindowMs); };
 const record = (selected: Stimulus | null) => { if (!base.current || seen.current) return; seen.current = true; const result = classifySelectiveTrial(base.current, selected, selected ? performance.now() : null, getSelectiveParams(audience, levelRef.current).responseWindowMs), all = [...trialsRef.current, result]; selectiveAttentionStore.saveTrial(result); trialsRef.current = all; setTrials(all); setFeedback(result.correct ? "Hedefi buldun!" : "Sıradaki hedefe odaklan."); const next = all.length % SELECTIVE_ATTENTION_BLOCK_SIZE === 0 ? getNextDifficulty(levelRef.current, all.slice(-10)) : levelRef.current; levelRef.current = next; setDifficulty(next); clear(); timer.current = setTimeout(() => show(next), 420); };
 useEffect(() => { if (phase !== "countdown") return; if (!count) { endAt.current = performance.now() + SELECTIVE_ATTENTION_SESSION_SECONDS * 1000; setPhase("playing"); show(); return; } const id = setTimeout(() => setCount(n => n - 1), 700); return () => clearTimeout(id); }, [phase, count]); useEffect(() => { if (phase !== "playing") return; const id = setInterval(() => setRemaining(Math.max(0, Math.ceil((endAt.current - performance.now()) / 1000))), 250); return () => clearInterval(id); }, [phase]); useEffect(() => () => clear(), []);
 if (phase === "intro") return <section className="focus-game"><GameArtwork game="selective-attention" audience={audience} aspect="hero" priority alt="Gürültüde Hedef oyun dünyası" className="focus-game__hero" /><p className="eyebrow">Gürültüde Hedef</p><h1>{audience === "kids" ? "Doğru hedefi bul!" : "Kalabalığın içinden hedefi ayırt et."}</h1><p>Renk ve şekil ipucunu birlikte kullan. Hedef olmayan işaretleri bekle.</p><Button variant="secondary" onClick={() => { setPhase("countdown"); setCount(3); }}>Başla</Button></section>;
 if (phase === "countdown") return <section className="focus-game focus-game--countdown"><p>Hedef kuralına hazır ol</p><strong>{count || "Başla!"}</strong></section>;
 if (phase === "complete") { const summary = buildSelectiveSummary(sessionId.current, audience, trials, difficulty); return <section className="focus-game"><GameArtwork game="selective-attention" audience={audience} aspect="hero" alt="Gürültüde Hedef tamamlandı" className="focus-game__hero" /><p className="eyebrow">Görev tamamlandı</p><h1>Görev performansın</h1><div className="focus-game__summary"><span><b>{summary.accuracyPercent ?? "—"}%</b>Doğruluk</span><span><b>{summary.wrongTargetErrors}</b>Yanlış seçim</span><span><b>{summary.selectiveAttentionScore ?? "Yetersiz veri"}</b>Seçici Dikkat</span></div><Button variant="secondary" onClick={() => window.location.reload()}>Yeni tur</Button></section>; }
 const size = getSelectiveParams(audience, difficulty).stimulusSize; return <section className="focus-game focus-game--playing"><header><span>Gürültüde Hedef</span><b>{Math.ceil(remaining / 60)} dk</b></header><ProgressBar value={(SELECTIVE_ATTENTION_SESSION_SECONDS - remaining) / SELECTIVE_ATTENTION_SESSION_SECONDS * 100} label="Oturum ilerlemesi" /><div className="selective-rule">{rule}</div><div className="selective-field"><p>{feedback || "İpucuna uygun hedefi seç."}</p><div>{stimuli.map(stimulus => <button key={stimulus.id} onClick={() => record(stimulus)} style={{ width: size, height: size }} className={`selective-stimulus selective-stimulus--${stimulus.color} selective-stimulus--${stimulus.shape}`} aria-label={`${stimulus.color} ${stimulus.shape}`}>{stimulus.shape === "star" ? "★" : stimulus.shape === "circle" ? "●" : stimulus.shape === "triangle" ? "▲" : "■"}</button>)}</div></div><small>Seviye {difficulty} · {trials.length} görev</small></section>; }
