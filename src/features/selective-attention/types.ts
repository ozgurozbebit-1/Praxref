export type SelectiveAttentionAudience = "kids" | "teen"; export type TrialType = "feature" | "conjunction" | "similarity"; export type Shape = "star" | "circle" | "triangle" | "square"; export type Color = "blue" | "gold" | "purple" | "coral"; export type ErrorType = "omission" | "wrong-target" | "late-response" | null;
export type Stimulus = { id: string; color: Color; shape: Shape; isTarget: boolean };
export type SelectiveAttentionTrial = { trialId: string; sessionId: string; audience: SelectiveAttentionAudience; difficultyLevel: number; trialType: TrialType; targetRule: string; targetStimulus: Stimulus; distractorCount: number; distractorSimilarity: number; appearedAt: number; respondedAt: number | null; reactionTimeMs: number | null; selectedStimulus: Stimulus | null; responded: boolean; correct: boolean; errorType: ErrorType; reactionTimeQualityFlag: "too-fast" | "invalid" | null };
export type SelectiveAttentionSummary = { sessionId: string; audience: SelectiveAttentionAudience; totalTrials: number; correctSelections: number; wrongTargetErrors: number; omissionErrors: number; lateResponses: number; accuracyPercent: number | null; meanReactionTimeMs: number | null; medianReactionTimeMs: number | null; reactionTimeSD: number | null; reactionTimeCV: number | null; averageDistractorCount: number; conjunctionAccuracy: number | null; highSimilarityAccuracy: number | null; selectiveAttentionScore: number | null; currentDifficulty: number; insufficientData: boolean };
